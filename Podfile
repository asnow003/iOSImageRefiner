# Uncomment the next line to define a global platform for your project
platform :ios, '11.3'

target 'iOSImageRefiner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOSImageRefiner
  pod 'AppCenter'

  target 'iOSImageRefinerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOSImageRefinerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
